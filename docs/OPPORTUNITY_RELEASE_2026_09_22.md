# Opportunity-review and pilot release

Baseline: accepted PR #107 main `c9aac8dc9aefd5c537a749abfe60a032979d0b66`. The prior approved `686d8b9fe11cb026edc9d0348a8adeb4aff627b9` is preserved on `baseline/accepted-686d8b9-20260922`.

## Delivered scope

An additive Review queue separates recent public evidence, qualified-for-investigation records and historical context. Cards explain what was documented, why it merits investigation and the next action, with dossier links, source dates, retrieval clocks and hashes. Existing routes, procurement data, historical cases, score gates, CSS and worklist storage remain unchanged. Recent is computed from the public date, never the website build or retrieval clock. An unavailable register or history does not become zero qualification coverage. All three data feeds fail independently; saved work is preserved.

The original six source-local project objects are preserved exactly by canonical SHA-256 `6777f23591c1263da741133f69de62757ca256b1267a7663afb1dc3240a80125`. Nine accepted PR #107 documentary projects extend this to fifteen dossiers. The source artifact and every retained raw response are rehashed before deterministic output is accepted. No first-entry, independent-holdout, current-operations or predictive claim is added.

Twenty source-backed triage decisions identify what remains unknown and the next check. All twenty remain HOLD because the required independent legal/group, civilian, current-status and source-reuse qualifications are incomplete. This is not twenty qualified leads. Every supported check is bound to an actual retained source; qualification needs all checks supported plus dated current-status evidence and expires back to review after 90 days. Test-only qualified fixtures never enter production data.

A fifty-target source-local review roster records exact source references and known-outcome selection bias. Thirteen rows reuse accepted historical research identity reviews, but those do not establish current commercial qualification. No fifty-company monitoring or independent prospective holdout is claimed. The existing eight-source operational monitor and its original observation clocks are untouched.

## Pilot preparation

`PILOT_OPERATOR_PACKET_2026_09_22.md` extends the existing pilot protocol with an invitation draft, consent and task-record contracts, source ablations, counterbalanced manual comparisons and private result analysis. The participant template contains no invented people. `scripts/analyse_pilot.py` reuses the existing review metrics per independently consented reviewer, separates duplicate alerts from review observations, excludes unverified reviewers, preserves unknown identity accuracy, requires manually recorded active-time pairs, and distinguishes willingness to pay from verified payment. Page-view time is never relabelled time saved. Real enrollment, paid value and predictive validation remain unachieved.

## Acceptance

Local full regression results: 397 Python and 60 JavaScript tests pass. Deterministic generation from exact accepted documentary artifact 10724340100 and monitor artifact 10720884358 passes. A dedicated proof workflow re-downloads/re-hashes both and compares every generated byte. Multi-device browser acceptance exercises all existing routes and every dossier plus queue views, URL search/reload, actual source links, saved actions, unavailable feeds, exact deployed assets and horizontal fit.

Do not merge until exact-head CI, workflow syntax, the source-bound pilot proof and UI usability acceptance pass and their exported artifacts are inspected. After merge, require exact-main Pages deployment and full hosted acceptance. Source count, a roster and passing tests are not commercial validation.
